[
  {
    "name": "/referrals shows sign-up prompt for new users",
    "steps": [
      {
        "send": { "text": "/referrals" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "You haven't signed up yet. Tap /start to create your account."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "referrals:show callback shows sign-up prompt for new users",
    "steps": [
      {
        "send": { "callback": "referrals:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "You haven't signed up yet. Tap /start to create your account."
            }
          }
        ]
      }
    ]
  }
]
